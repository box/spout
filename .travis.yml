language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - nightly
  - hhvm

script: phpunit

before_script:
  - composer self-update
  - composer install --dev --prefer-source
  - mkdir -p build/logs

after_script:
  - php vendor/bin/coveralls -v
